<section class="deliverables section" id="entregables">
  <div class="container-wide">
    <div class="section-heading" data-reveal>
      <div class="eyebrow">Output credibility</div>
      <div>
        <h2 class="section-title">La entrega no termina en un deck bonito.</h2>
        <p class="section-copy">
          Lo que se entrega debe servirle a quien decide, a quien implementa despues
          y a quien necesita reconstruir por que esa recomendacion existe.
        </p>
      </div>
    </div>

    <div class="deliverable-grid" data-reveal-stagger>
      <article class="deliverable-card surface-card">
        <small>Dossier de escenarios</small>
        <h3>La pieza central es un mapa, no un slogan.</h3>
        <p>Escenarios plausibles, implicancias, comparacion entre mundos y una lectura ejecutiva conversable.</p>
      </article>

      <article class="deliverable-card surface-card">
        <small>Evidence trail</small>
        <h3>Cada hallazgo deja rastro.</h3>
        <p>Fuentes, thresholds, relaciones y supuestos visibles para que la recomendacion tenga base y no misticismo.</p>
      </article>

      <article class="deliverable-card surface-card">
        <small>Signposts</small>
        <h3>Senales para monitorear antes de reaccionar tarde.</h3>
        <p>Umbrales concretos que ayudan a detectar cuando el entorno esta desplazando el escenario dominante.</p>
      </article>

      <article class="deliverable-card surface-card">
        <small>Plan a 90 dias</small>
        <h3>La salida termina en accion coordinada.</h3>
        <p>Palancas, prioridades y decisiones secuenciadas para que la conversacion no muera al cerrar la reunion.</p>
      </article>
    </div>
  </div>
</section>

<style>
  .deliverable-grid {
    display: grid;
    gap: 1rem;
  }

  .deliverable-card {
    padding: 1.45rem;
  }

  .deliverable-card small {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: 0.65rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--ink-faint);
  }

  .deliverable-card h3 {
    margin: 0.8rem 0 0;
    font-size: clamp(1.35rem, 1.08rem + 0.8vw, 1.95rem);
    line-height: 1.08;
    letter-spacing: -0.04em;
  }

  .deliverable-card p {
    margin: 0.8rem 0 0;
    color: var(--ink-soft);
  }

  @media (min-width: 900px) {
    .deliverable-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }
</style>
