<section class="audiences section" id="audiencias">
  <div class="container-wide">
    <div class="section-heading" data-reveal>
      <div class="eyebrow">For whom</div>
      <div>
        <h2 class="section-title">La pagina habla primero a quienes ya sienten el costo de decidir tarde.</h2>
        <p class="section-copy">
          Dasein esta pensado para pilotos donde la adopcion de IA, el cambio regulatorio o
          la friccion institucional vuelven caro improvisar. Partners y colaboradores entran,
          pero despues del problema central.
        </p>
      </div>
    </div>

    <div class="audience-grid" data-reveal-stagger>
      <article class="audience-card audience-card--primary surface-card">
        <small>Pilots</small>
        <h3>Organizaciones con una decision que ya tiene consecuencias operativas, politicas o reputacionales.</h3>
        <p>
          Cuando una mala lectura del entorno puede afectar operaciones, ventas, clientes o legitimidad,
          un piloto bien formulado vale mas que una narrativa optimista.
        </p>
        <ul>
          <li>Adopcion de IA con friccion institucional</li>
          <li>Entornos regulados o con actores de veto</li>
          <li>Lanzamientos con costo reputacional alto</li>
        </ul>
      </article>

      <article class="audience-card surface-card-soft">
        <small>Partners</small>
        <h3>Equipos que entran despues del mapa.</h3>
        <p>
          Transformacion, implementacion, cambio, IA o producto: Dasein ordena el terreno y
          los partners ejecutan donde el trabajo estrategico termina.
        </p>
      </article>

      <article class="audience-card surface-card-soft">
        <small>Collaborators</small>
        <h3>Perfiles capaces de elevar la lectura.</h3>
        <p>
          Research, foresight, product, computational social science y personas que entienden
          sistemas complejos sin perder el criterio operativo.
        </p>
      </article>
    </div>
  </div>
</section>

<style>
  .audience-grid {
    display: grid;
    gap: 1rem;
  }

  .audience-card {
    padding: 1.45rem;
  }

  .audience-card small {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: 0.66rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--ink-faint);
  }

  .audience-card h3 {
    margin: 0.85rem 0 0;
    font-size: clamp(1.28rem, 1rem + 0.8vw, 2.05rem);
    line-height: 1.06;
    letter-spacing: -0.04em;
  }

  .audience-card p {
    margin: 0.85rem 0 0;
    color: var(--ink-soft);
  }

  .audience-card ul {
    display: grid;
    gap: 0.65rem;
    margin: 1rem 0 0;
    padding-left: 1.1rem;
    color: var(--ink-soft);
  }

  .audience-card--primary {
    background:
      radial-gradient(circle at top right, rgba(72, 101, 255, 0.1), transparent 25%),
      rgba(255, 255, 255, 0.58);
  }

  @media (min-width: 980px) {
    .audience-grid {
      grid-template-columns: minmax(0, 1.2fr) repeat(2, minmax(0, 0.8fr));
      align-items: stretch;
    }

    .audience-card--primary {
      grid-column: span 1;
      min-height: 100%;
    }
  }
</style>
