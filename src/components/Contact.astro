<section class="cta-section section-dark" id="contacto">
  <div class="container">

    <!-- Editorial split: massive headline left, action right -->
    <div class="cta-grid" data-reveal>
      <div class="cta-left">
        <h2 class="cta-headline">
          Hablemos<br />del <em>caso.</em>
        </h2>
      </div>

      <div class="cta-right">
        <p class="cta-body">
          Una conversación breve basta para entender si el caso necesita
          un pulso estratégico, una simulación completa o una colaboración
          posterior con partners.
        </p>

        <div class="cta-actions">
          <a class="cta-primary" href="mailto:salvatierra.pablom@gmail.com">
            Agenda una conversación
            <span class="cta-arrow" aria-hidden="true">&rarr;</span>
          </a>
          <a class="cta-link" href="https://wa.me/56939056565" target="_blank" rel="noreferrer">
            O escríbenos por WhatsApp
          </a>
        </div>

        <!-- Inline contact details -->
        <div class="cta-details">
          <div class="cta-detail">
            <small>Email</small>
            <a href="mailto:salvatierra.pablom@gmail.com">salvatierra.pablom@gmail.com</a>
          </div>
          <div class="cta-detail">
            <small>Teléfono</small>
            <a href="https://wa.me/56939056565" target="_blank" rel="noreferrer">+56 9 3905 6565</a>
          </div>
          <div class="cta-detail">
            <small>Base</small>
            <span>Santiago, Chile</span>
          </div>
        </div>
      </div>
    </div>

  </div>

  <!-- Minimal footer -->
  <footer class="cta-footer">
    <div class="container cta-footer-inner">
      <span class="cta-footer-brand">Dasein Foresight</span>
      <nav class="cta-footer-links" aria-label="External links">
        <a href="https://github.com/SalvatierraPM" target="_blank" rel="noreferrer">GitHub</a>
        <a href="https://substack.com/@pavlogalgo" target="_blank" rel="noreferrer">Substack</a>
      </nav>
      <span class="cta-footer-copy">&copy; 2025</span>
    </div>
  </footer>
</section>

<style>
  .cta-section {
    padding-top: clamp(8rem, 6rem + 8vw, 16rem);
    padding-bottom: 0;
  }

  /* ─── Editorial split grid ─── */
  .cta-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(3rem, 5vw, 6rem);
    align-items: start;
  }

  /* Left: massive headline */
  .cta-headline {
    font-family: var(--font-display);
    font-size: clamp(4rem, 2rem + 8vw, 9rem);
    font-weight: 600;
    line-height: 0.88;
    letter-spacing: -0.05em;
    color: var(--slate-white);
  }

  .cta-headline em {
    font-family: var(--font-editorial);
    font-style: italic;
    font-weight: 400;
    color: var(--signal-red);
  }

  /* Right: body + CTA + details */
  .cta-right {
    display: grid;
    gap: clamp(2rem, 3vw, 3rem);
    padding-top: 0.8em;
  }

  .cta-body {
    color: rgba(243, 245, 248, 0.56);
    font-size: clamp(0.95rem, 0.9rem + 0.25vw, 1.08rem);
    line-height: 1.72;
    max-width: 36ch;
  }

  /* Actions */
  .cta-actions {
    display: grid;
    gap: 1rem;
  }

  .cta-primary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.8rem;
    width: fit-content;
    font-family: var(--font-mono);
    font-size: var(--text-caption);
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    padding: 1.1rem 2.5rem;
    background: var(--slate-white);
    color: var(--navy-deep, #0a1628);
    border: 1px solid var(--slate-white);
    text-decoration: none;
    transition: transform var(--dur-normal) var(--ease-out-expo),
                gap var(--dur-normal) var(--ease-out-expo);
  }

  .cta-primary:hover {
    transform: translateY(-2px);
    gap: 1.2rem;
  }

  .cta-primary:focus-visible {
    outline: 2px solid var(--signal-red);
    outline-offset: 3px;
  }

  .cta-arrow {
    display: inline-block;
    transition: transform var(--dur-normal) var(--ease-out-expo);
  }

  .cta-primary:hover .cta-arrow {
    transform: translateX(4px);
  }

  .cta-link {
    font-size: var(--text-sm);
    color: rgba(243, 245, 248, 0.48);
    text-decoration: none;
    border-bottom: 1px solid rgba(243, 245, 248, 0.15);
    width: fit-content;
    padding-bottom: 2px;
    transition: color var(--dur-fast) ease, border-color var(--dur-fast) ease;
  }

  .cta-link:hover {
    color: var(--slate-white);
    border-color: rgba(243, 245, 248, 0.4);
  }

  /* Contact details — horizontal row */
  .cta-details {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0;
    border-top: 1px solid rgba(243, 245, 248, 0.08);
    padding-top: clamp(1.5rem, 2vw, 2rem);
  }

  .cta-detail {
    display: grid;
    gap: 0.3rem;
  }

  .cta-detail:not(:first-child) {
    padding-left: 1.5rem;
    border-left: 1px solid rgba(243, 245, 248, 0.06);
  }

  .cta-detail small {
    font-family: var(--font-mono);
    font-size: 0.6rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: rgba(243, 245, 248, 0.3);
  }

  .cta-detail a,
  .cta-detail span {
    color: rgba(243, 245, 248, 0.62);
    font-size: 0.82rem;
    text-decoration: none;
    transition: color var(--dur-fast) ease;
  }

  .cta-detail a:hover {
    color: var(--slate-white);
  }

  /* ─── Minimal footer ─── */
  .cta-footer {
    margin-top: clamp(6rem, 5rem + 5vw, 12rem);
    border-top: 1px solid rgba(243, 245, 248, 0.06);
    padding: clamp(1.5rem, 2vw, 2.5rem) 0;
  }

  .cta-footer-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }

  .cta-footer-brand {
    font-family: var(--font-display);
    font-size: 0.82rem;
    font-weight: 600;
    color: rgba(243, 245, 248, 0.5);
    letter-spacing: -0.01em;
  }

  .cta-footer-links {
    display: flex;
    gap: 1.5rem;
  }

  .cta-footer-links a {
    font-size: 0.75rem;
    color: rgba(243, 245, 248, 0.36);
    text-decoration: none;
    transition: color var(--dur-fast) ease;
  }

  .cta-footer-links a:hover {
    color: var(--slate-white);
  }

  .cta-footer-copy {
    font-family: var(--font-mono);
    font-size: 0.6rem;
    letter-spacing: 0.08em;
    color: rgba(243, 245, 248, 0.2);
  }

  /* ─── Mobile ─── */
  @media (max-width: 860px) {
    .cta-grid {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    .cta-headline {
      font-size: clamp(3.2rem, 2rem + 6vw, 5.5rem);
    }

    .cta-details {
      grid-template-columns: 1fr;
      gap: 1.2rem;
    }

    .cta-detail:not(:first-child) {
      padding-left: 0;
      border-left: none;
      padding-top: 1.2rem;
      border-top: 1px solid rgba(243, 245, 248, 0.06);
    }

    .cta-footer-inner {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.8rem;
    }
  }

  /* ─── Reduced motion ─── */
  @media (prefers-reduced-motion: reduce) {
    .cta-primary,
    .cta-arrow,
    .cta-link {
      transition: none;
    }

    .cta-primary:hover,
    .cta-primary:hover .cta-arrow {
      transform: none;
    }
  }
</style>
